# 依赖
node_modules/
**/node_modules/

# 构建产物
dist/
build/
client/dist/
client/.vite/

# 环境变量（不要提交）
.env
.env.local
.env.*.local
api/.env
client/.env

# 日志、临时
*.log
*.logs
*.swp
.DS_Store

# 锁文件：npm 建议保留 package-lock.json（不要忽略）
# yarn.lock / pnpm-lock.yaml 二选一保留，使用哪个就提交哪个
